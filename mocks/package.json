{
  "name": "mock-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 5000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "dependencies": {
    "react-full-stack": "file:.."
  }
}
