{
  "name": "react-graphql-fullstack",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server && npm install --prefix mocks",
    "mock": "json-server --watch mocks/db.json --port 5000",
    "graphql": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run mock\" \"npm run graphql\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  }
}
